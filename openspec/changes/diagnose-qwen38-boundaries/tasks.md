# Tasks

- [ ] Freeze model-facing cases, separate oracle, and their hashes.
- [ ] Validate the fixture without model inference.
- [ ] Run one bounded GPU job on the pinned Qwen3.8 revision.
- [ ] Preserve and verify raw output and runtime metadata before scoring.
- [ ] Score per case and panel; report limitations without changing the active
  default, model configuration, prompt, or historical results.
- [ ] Run focused tests and strict OpenSpec validation.
